# Runtime dependencies for the AI Creative Copy Agent backend.
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
email-validator>=2.2.0
SQLAlchemy>=2.0.35
alembic>=1.13.0
PyJWT>=2.9.0
bcrypt>=4.1.0
python-multipart>=0.0.9

# Optional: only imported when AI_PROVIDER=gemini or GROUNDING_PROVIDER=gemini.
# Remove this line if you only ever run the mock provider.
google-genai>=1.0.0
