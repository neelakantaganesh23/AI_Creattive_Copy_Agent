# Runtime dependencies for the AI Creative Copy Agent backend.
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
email-validator>=2.2.0
SQLAlchemy>=2.0.35
alembic>=1.13.0
PyJWT>=2.9.0
bcrypt>=4.1.0
python-multipart>=0.0.9
# Used by the Tavily grounding provider.
httpx>=0.27.0

# The agent framework. Pinned to a minor version: its API surface still moves
# faster than the rest of the stack. The [google] extra pulls in google-genai,
# which the Gemini model and the Gemini grounding provider both use.
pydantic-ai-slim[google]>=2.29,<2.30
